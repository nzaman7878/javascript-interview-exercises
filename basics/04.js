// Create a funtion to calculate the area of a rectangle given its widht and height

let calculateAreaOfRectangle = (width,height) => {

    return width * height;

}

let area = calculateAreaOfRectangle(5,7);

console.log("Area of the Rectangle is :" + area);
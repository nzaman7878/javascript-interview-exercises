// Use map() to double all the elements in an array

function doubleArrayElements(arr) {
    return arr.map(num =>num * 2)}
   
    
    let arr = [19,39,40,60,70]

    console.log(doubleArrayElements(arr))